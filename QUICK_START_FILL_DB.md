# 🚀 Быстрый старт: Наполнение базы данных

**Цель:** Импортировать первые протоколы забегов за 30-60 минут

---

## Шаг 1: Подготовка (5 минут)

### 1.1 Проверить зависимости

```bash
cd bot
pip install -r requirements.txt
```

Нужные библиотеки должны быть установлены:
- `pdfplumber` (для PDF)
- `pandas` и `openpyxl` (для Excel)
- `aiosqlite` (для БД)

### 1.2 Создать папку для протоколов (если нет)

```bash
mkdir bot/protocols
```

---

## Шаг 2: Скачать первый протокол (10 минут)

### Вариант A: 5верст (самый простой)

1. Открой: https://5verst.ru/results
2. Выбери любой забег (например, "Пятерка в Парке")
3. Скачай протокол (PDF или Excel)
4. Сохрани в `bot/protocols/` с понятным названием:
   ```
   bot/protocols/5verst_pjaterka_2026-02-22.pdf
   ```

### Вариант B: Другой организатор

- **RHR:** https://rhr-marathon.ru/results
- **S95:** https://s95.run/results
- **Московский марафон:** https://moscowmarathon.ru/results

---

## Шаг 3: Импорт протокола (5-10 минут)

### Способ 1: Интерактивный скрипт (рекомендуется)

```bash
cd bot/scripts
python quick_import.py
```

Скрипт попросит ввести:
- Путь к файлу
- Название забега
- Дату забега
- Место проведения
- Организатора
- Дистанцию
- И другие параметры

**Пример:**
```
Путь к файлу: ../protocols/5verst_pjaterka_2026-02-22.pdf
Название забега: Пятерка в Парке
Дата забега: 2026-02-22
Место проведения: Москва, Парк Горького
Организатор: 5верст
Тип забега: шоссе
Дистанция: 5 км
```

### Способ 2: Прямой вызов через Python

Создай файл `bot/scripts/test_import.py`:

```python
import asyncio
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent.parent.parent))

from bot.scripts.parse_protocol import ProtocolImporter
from bot.db import db

async def main():
    await db.connect()
    importer = ProtocolImporter()
    
    await importer.import_protocol(
        file_path="bot/protocols/5verst_pjaterka_2026-02-22.pdf",
        race_name="Пятерка в Парке",
        race_date="2026-02-22",
        race_location="Москва, Парк Горького",
        race_organizer="5верст",
        race_type="шоссе",
        distance="5 км",
        header_row=0
    )
    
    await db.disconnect()

asyncio.run(main())
```

Запусти:
```bash
cd bot/scripts
python test_import.py
```

---

## Шаг 4: Проверка результата (5 минут)

### 4.1 Проверить в базе данных

```bash
cd bot
python -c "import asyncio; from bot.db import db; asyncio.run(db.connect()); print('Забегов:', asyncio.run(db.db.fetchval('SELECT COUNT(*) FROM races'))); print('Результатов:', asyncio.run(db.db.fetchval('SELECT COUNT(*) FROM results'))); asyncio.run(db.disconnect())"
```

Или через SQLite:
```bash
sqlite3 bot/seido.db "SELECT COUNT(*) FROM races;"
sqlite3 bot/seido.db "SELECT COUNT(*) FROM results;"
```

### 4.2 Проверить в боте

1. Запусти бота (если не запущен)
2. Открой бота в Telegram
3. Попробуй найти результат:
   - Команда `/myresults` (если зарегистрирован)
   - Или поиск по ФИО из протокола

---

## 🎯 Цель на первый раз

- ✅ Импортировать 1 протокол
- ✅ Получить 100+ результатов в базе
- ✅ Убедиться, что поиск работает

---

## 📝 Что дальше?

После успешного первого импорта:

1. **Скачай ещё 5-10 протоколов** разных организаторов
2. **Импортируй их** через `quick_import.py`
3. **Проверь качество данных** (нет ли дубликатов, ошибок)
4. **Запусти автоматические парсеры** для онлайн-протоколов

---

## 🆘 Проблемы и решения

### Ошибка: "Файл не найден"
**Решение:** Проверь путь к файлу. Используй относительный путь от `bot/scripts/` или абсолютный.

### Ошибка: "Не удалось извлечь данные"
**Решение:** 
- Проверь формат файла (PDF или Excel)
- Попробуй указать другой `header_row` (0, 1, 2)
- Для Excel — укажи `sheet_name`

### Ошибка: "Нет модуля pdfplumber"
**Решение:**
```bash
pip install pdfplumber pandas openpyxl
```

### Результаты не находятся в боте
**Решение:**
- Проверь, что данные действительно импортировались (через SQL)
- Проверь формат ФИО в протоколе
- Попробуй поиск по части фамилии

---

## 📊 Ожидаемый результат

После импорта одного протокола:
- ✅ 1 забег в базе
- ✅ 100-1000+ результатов (зависит от размера забега)
- ✅ 100-1000+ бегунов (если все новые)
- ✅ Поиск работает

---

**Время:** 30-60 минут на первый импорт  
**Сложность:** ⭐⭐ (средняя)

*Удачи! 🏃‍♂️*
