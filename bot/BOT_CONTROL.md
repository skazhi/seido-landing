# 🚀 Управление ботом Seido

## Быстрый старт

### Запуск бота

**Вариант 1: Batch файл (рекомендуется, работает всегда)**
```cmd
cd "C:\Users\PC User\Documents\Seido\bot"
start_bot.bat
```

Или просто двойной клик по файлу `start_bot.bat`

**Вариант 2: Простой запуск**
```cmd
cd "C:\Users\PC User\Documents\Seido\bot"
start_bot_simple.bat
```

**Вариант 3: PowerShell (если политика выполнения разрешена)**
```powershell
cd "C:\Users\PC User\Documents\Seido\bot"
.\start_bot.ps1
```

**Вариант 4: Вручную через Python**
```powershell
cd "C:\Users\PC User\Documents\Seido\bot"
python main.py
```

### Остановка бота

**Способ 1: В терминале (где запущен бот)**
- Нажмите `Ctrl+C`

**Способ 2: PowerShell (если политика выполнения разрешена)**
```powershell
cd "C:\Users\PC User\Documents\Seido\bot"
.\stop_bot.ps1
```

**Способ 3: Через Диспетчер задач**
- Откройте Диспетчер задач (Ctrl+Shift+Esc)
- Найдите процесс `python.exe`
- Завершите процесс

**Способ 4: Через командную строку**
```cmd
taskkill /F /IM python.exe
```
⚠️ Внимание: это остановит ВСЕ процессы Python!

### Просмотр логов

**Способ 1: Открыть файл вручную**
- Перейдите в папку `bot\logs\`
- Откройте файл `bot_YYYYMMDD.log` (где YYYYMMDD - текущая дата)
- Можно открыть в любом текстовом редакторе (Блокнот, Notepad++, VS Code)

**Способ 2: PowerShell (если политика выполнения разрешена)**
```powershell
cd "C:\Users\PC User\Documents\Seido\bot"
.\view_logs.ps1
```

**Способ 3: Через командную строку (последние 50 строк)**
```cmd
cd "C:\Users\PC User\Documents\Seido\bot\logs"
for /f "delims=" %f in ('dir /b /o-d bot_*.log') do @more +50 "%f" & goto :eof
```

## 📁 Структура логов

Логи сохраняются в папке `bot\logs\`:
- Формат имени: `bot_YYYYMMDD.log` (один файл на день)
- Кодировка: UTF-8
- Формат: `YYYY-MM-DD HH:MM:SS - LEVEL - сообщение`

## ⚠️ Важные замечания

1. **Только один экземпляр!** 
   - Telegram API не позволяет запускать несколько экземпляров одного бота
   - Перед запуском убедитесь, что предыдущий процесс остановлен

2. **Проверка запущенных процессов:**
   ```powershell
   Get-Process python -ErrorAction SilentlyContinue
   ```

3. **Если бот не отвечает:**
   - Проверьте логи на наличие ошибок
   - Убедитесь, что токен в `.env` правильный
   - Проверьте интернет-соединение

## 🔧 Устранение проблем

### Конфликт экземпляров (TelegramConflictError)
Если видите ошибку `TelegramConflictError: terminated by other getUpdates request`:

**Способ 1: Batch файл (рекомендуется)**
```cmd
cd "C:\Users\PC User\Documents\Seido\bot"
stop_all_python.bat
```

**Способ 2: PowerShell**
```powershell
# Остановите все процессы Python
Get-Process python -ErrorAction SilentlyContinue | Stop-Process -Force

# Подождите 2 секунды
Start-Sleep -Seconds 2

# Затем запустите бота заново
cd "C:\Users\PC User\Documents\Seido\bot"
.\start_bot.bat
```

**Способ 3: Через командную строку**
```cmd
taskkill /F /IM python.exe
```
⚠️ Внимание: это остановит ВСЕ процессы Python на компьютере!

### Просмотр последних ошибок
```powershell
# Последние 100 строк лога
Get-Content bot\logs\bot_*.log -Tail 100 | Select-String -Pattern "ERROR|Exception|Traceback"
```

## 📝 Примеры команд

**Запуск с выводом в консоль:**
```powershell
python main.py
```

**Запуск в фоне (Windows):**
```powershell
Start-Process python -ArgumentList "main.py" -WindowStyle Hidden
```

**Проверка статуса:**
```powershell
Get-Process python -ErrorAction SilentlyContinue | Format-Table Id, ProcessName, StartTime
```
