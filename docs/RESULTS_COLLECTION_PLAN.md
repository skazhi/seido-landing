# План сбора результатов забегов

## Цель
Максимально собрать результаты завершённых забегов в БД, привязать к организаторам, обеспечить поиск и автоматическую/ручную привязку к бегунам.

---

## Архитектура

1. **Источники протоколов** — URL протоколов (PDF, HTML, Excel) с сайтов организаторов
2. **Сборщик** — загрузка, парсинг, нормализация, импорт в БД
3. **Матчинг** — сопоставление строк протокола с бегунами в БД (ФИО, дата рождения)
4. **Заявка «это я»** — если автомат не нашёл, бегун может заявить результат → на рассмотрение админу

---

## Связь с организаторами и ссылками

- Каждый забег: `organizer`, `website_url`, `protocol_url`
- Результат наследует ссылку на протокол через `race_id` → `races.protocol_url`
- Поиск: по названию забега, организатору, дате, дистанции, ФИО бегуна

---

## Логика матчинга

1. При импорте строки протокола: ищем бегуна по `last_name`, `first_name`, `birth_date` (если есть)
2. **Приоритет**: бегун с `telegram_id` (зарегистрирован) > бегун без
3. Если совпадений несколько — не привязываем, сохраняем как «непривязанный» (runner без telegram_id)
4. Если не нашли — создаём runner без telegram_id, добавляем result

---

## Заявка «это я»

1. Бегун ищет результат (поиск по ФИО, забегу)
2. Находит себя в непривязанных или «чужом» runner
3. Нажимает «Это я» → создаётся заявка в `result_claims`
4. Админ получает уведомление, проверяет, одобряет/отклоняет
5. При одобрении: `result.runner_id` = runner_id бегуна (или мердж профилей)

---

## Источники протоколов (по организаторам)

| Организатор | Источник | Формат | Статус |
|-------------|----------|--------|--------|
| RussiaRunning | results.russiarunning.com/event/{id} | HTML (SPA) | URL сохраняем, парсер HTML — TODO |
| IronStar | iron-star.com | HTML | Парсер нужен |
| TIMERMAN | kazan.run, timerman.org | HTML/PDF | Парсер нужен |
| Беговое сообщество | runc.run, whitenightsmarathon | PDF | parse_protocol |
| RHR | goldenultra.ru | — | Уточнить |
| Orgeo | orgeo.ru | API/HTML | Много забегов |
| 5верст | 5verst.ru | HTML | По локациям |
| S95 | s95.ru | HTML | Парсер нужен |
| Высшая лига | topliga.ru | HTML | Парсер нужен |
| TulaMarathon | tulamarathon.org | HTML/PDF | Парсер нужен |

**Стратегия:** для каждого источника — отдельный парсер. Если не парсится — пропуск, в лог. Собираем всё, что можно.

---

## Реализованные компоненты

- [x] `result_claims` — таблица заявок
- [x] Логика матчинга (приоритет runner с telegram_id)
- [x] «🔎 Найти результат» — поиск по ФИО, кнопка «Это я»
- [x] Заявка «Это я» → уведомление админу
- [x] `/admin_claims` — рассмотрение заявок (одобрить/отклонить)
- [x] `bot/scripts/collect_results.py` — загрузка протоколов по URL из БД и импорт
- [ ] Добавить `protocol_url` к забегам в парсерах (где доступно)
- [ ] Запуск collect_results по расписанию (cron: `python -m bot.scripts.collect_results`)
- [ ] HTML-парсер для results.russiarunning.com (SPA — нужен Playwright или API)

---

## Первый прогон (21.02.2026)

**Период:** 01.01.2025 — 20.02.2026  
**Скрипт:** `python -m bot.scripts.discover_and_collect_protocols`

| Метрика | Значение |
|---------|----------|
| RussiaRunning: прошедших забегов (API) | 1000 |
| Забегов в БД всего | ~1535 |
| С protocol_url (results.russiarunning.com) | ~950 |
| Результатов импортировано | 0 |

**Причина 0 результатов:** `results.russiarunning.com` — HTML (SPA), данные подгружаются JS. Текущий парсер работает только с PDF/Excel. Для импорта нужен HTML-парсер или reverse-engineering API results.