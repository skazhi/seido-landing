# 🏃 Жизненный цикл забега в Seido

## Концепция работы

### Статусы забега

Забег проходит через следующие стадии:

1. **📅 Анонс (Upcoming)** — забег ещё не состоялся
   - Дата забега в будущем
   - Показывается в календаре
   - Можно подписаться на уведомления

2. **⏳ Прошедший (Past)** — забег состоялся, но результатов ещё нет
   - Дата забега в прошлом
   - Протокол ещё не опубликован
   - Показывается в истории забегов

3. **✅ С результатами (Completed)** — забег состоялся, результаты добавлены
   - Дата забега в прошлом
   - Есть результаты в базе
   - Можно искать результаты участников

---

## Автоматический переход статусов

### Правило 1: По дате забега

```
Если date < сегодня → забег автоматически считается "прошедшим"
Если date >= сегодня → забег считается "анонсом"
```

**Реализация:** Определяется динамически при запросах, без изменения БД.

### Правило 2: По наличию результатов

```
Если есть результаты в таблице results для этого забега → статус "с результатами"
Если результатов нет → статус "прошедший" или "анонс"
```

**Реализация:** Проверяется при запросах через JOIN с таблицей results.

---

## Представления для пользователей

### 1. Календарь забегов (`/calendar`)

**Показывает:**
- Только анонсы (date >= сегодня)
- Сортировка по дате (ближайшие первыми)
- Фильтры: город, тип, дистанция

**Пример:**
```
📅 Предстоящие забеги:

🏁 Пятерка в Парке
📅 2026-02-22
📍 Москва, Парк Горького
🏢 Организатор: 5верст
🔗 https://5verst.ru/pjaterka

🏁 Московский марафон
📅 2026-04-15
📍 Москва, Лужники
🏢 Организатор: Московский марафон
```

### 2. История забегов (`/history` или `/past_races`)

**Показывает:**
- Прошедшие забеги (date < сегодня)
- С результатами и без
- Сортировка по дате (новые первыми)

**Пример:**
```
📜 История забегов:

🏁 Забег за мечтой ✅
📅 2026-01-15 (прошёл)
📍 Москва, ВДНХ
🏢 Организатор: RHR
📊 Результатов: 250
🔍 /results_race 123

🏁 Кросс Нации ⏳
📅 2026-01-10 (прошёл)
📍 Москва
🏢 Организатор: ФЛА
📊 Результатов: 0 (ожидаем протокол)
```

### 3. Поиск результатов (`/myresults` или `/search`)

**Показывает:**
- Только забеги с результатами
- Результаты конкретного бегуна
- Можно искать по фамилии

---

## Структура данных

### Текущая таблица races

```sql
CREATE TABLE races (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    date TEXT NOT NULL,              -- Дата забега
    location TEXT,
    organizer TEXT,
    race_type TEXT,
    distances TEXT,
    website_url TEXT,
    protocol_url TEXT,
    is_active INTEGER DEFAULT 1,     -- Актуален ли забег
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

### Предлагаемые улучшения

**Вариант 1: Добавить поле status (рекомендуется)**

```sql
ALTER TABLE races ADD COLUMN status TEXT DEFAULT 'upcoming';
-- status: 'upcoming', 'past', 'completed'
```

**Вариант 2: Определять динамически (проще, без миграции)**

Определять статус при запросах:
- `upcoming`: date >= сегодня AND results_count = 0
- `past`: date < сегодня AND results_count = 0
- `completed`: date < сегодня AND results_count > 0

---

## Логика работы

### Сценарий 1: Добавление анонса

1. Парсер находит забег на 22 февраля 2026
2. Добавляется в базу со статусом "upcoming" (или определяется по дате)
3. Показывается в `/calendar`
4. Пользователи могут подписаться на уведомления

### Сценарий 2: Прошла дата забега

1. 23 февраля 2026 (день после забега)
2. Забег автоматически перестаёт показываться в `/calendar`
3. Появляется в `/history` со статусом "past"
4. Показывается индикатор "⏳ Ожидаем протокол"

### Сценарий 3: Добавление протокола

1. Админ импортирует протокол через `/admin_import_protocol`
2. Добавляются результаты в таблицу results
3. Забег автоматически получает статус "completed"
4. В `/history` показывается "✅ С результатами"
5. Можно искать результаты через `/search` или `/myresults`

---

## Команды бота

### Для пользователей

- `/calendar` — предстоящие забеги (анонсы)
- `/history` — прошедшие забеги (с результатами и без)
- `/myresults` — мои результаты (только забеги с результатами)
- `/search <фамилия>` — поиск результатов по фамилии
- `/race <id>` — информация о конкретном забеге

### Для админов

- `/admin_import_protocol <race_id> <file>` — импорт протокола
- `/admin_race_status <race_id>` — проверить статус забега
- `/admin_update_race <race_id>` — обновить информацию о забеге

---

## Автоматизация

### Ежедневная задача

Каждый день в 00:00:
1. Проверяем все забеги с date < сегодня
2. Если результатов нет → статус "past"
3. Если результаты есть → статус "completed"
4. Отправляем уведомления подписчикам (если нужно)

### При добавлении результатов

1. Импорт протокола
2. Автоматически обновляется статус забега на "completed"
3. Уведомления подписчикам (если нужно)

---

## Визуальное представление

### Индикаторы статуса

- 📅 **Анонс** — забег в будущем
- ⏳ **Ожидаем протокол** — забег прошёл, результатов нет
- ✅ **С результатами** — забег прошёл, результаты есть
- 🔍 **Искать результаты** — кнопка для поиска

### Пример карточки забега

```
🏁 Пятерка в Парке
━━━━━━━━━━━━━━━━━━━━━━━━
📅 Дата: 22 февраля 2026
📍 Место: Москва, Парк Горького
🏢 Организатор: 5верст
🏃 Дистанции: 5 км, 10 км

📊 Статус: ✅ С результатами (250 участников)
🔗 Сайт: https://5verst.ru/pjaterka
📄 Протокол: https://5verst.ru/results

[🔍 Найти результаты] [📊 Статистика]
```

---

## Преимущества подхода

✅ **Автоматический переход** — не нужно вручную менять статусы  
✅ **Гибкость** — можно определять статус динамически  
✅ **Прозрачность** — пользователь видит, есть ли результаты  
✅ **Масштабируемость** — легко добавить новые статусы  
✅ **UX** — понятное разделение на анонсы и историю

---

## Реализация

### Шаг 1: Добавить методы в db.py

```python
async def get_races_by_status(self, status: str, limit: int = 10):
    """Получить забеги по статусу"""
    # status: 'upcoming', 'past', 'completed'
    
async def get_race_status(self, race_id: int) -> str:
    """Определить статус забега"""
    # Проверяет дату и наличие результатов
```

### Шаг 2: Обновить handlers.py

- Добавить команду `/history`
- Обновить `/calendar` для показа только анонсов
- Добавить индикаторы статуса

### Шаг 3: Автоматизация

- Задача в планировщике для ежедневной проверки
- Автоматическое обновление при импорте протокола

---

## Итог

**Забег автоматически переходит:**
- Анонс → Прошедший (по дате)
- Прошедший → С результатами (при импорте протокола)

**Пользователь видит:**
- Календарь — только будущие забеги
- История — прошедшие забеги (с индикатором наличия результатов)
- Результаты — только забеги с результатами

**Всё работает автоматически, без ручного вмешательства!** 🎉
