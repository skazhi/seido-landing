# Инструкция: Запуск Telegram-бота Seido

## Предварительные требования

1. ✅ Код бота написан (`bot/main.py`)
2. ✅ Схема БД готова (`bot/schema.sql`)
3. ✅ База данных заполнена тестовыми данными

## Шаги запуска

### 1. Получить токен бота

✅ **Бот уже создан:** @seidocommunitybot

Чтобы получить токен:
1. Открой Telegram
2. Найди бота **@BotFather**
3. Отправь команду `/mybots`
4. Выбери бота **@seidocommunitybot**
5. Нажми "API Token"
6. Скопируй токен (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Узнать свой Telegram ID

1. Найди бота **@userinfobot**
2. Отправь `/start`
3. Бот пришлёт твой ID (например, `123456789`)

### 3. Настроить окружение

1. Перейди в папку бота:
   ```bash
   cd bot
   ```

2. Создай файл `.env`:
   ```bash
   cp .env.example .env
   # или создай вручную
   ```

3. Открой `.env` и заполни:
   ```
   BOT_TOKEN=твой_токен_от_BotFather
   ADMIN_ID=твой_telegram_id
   ```

### 4. Установить зависимости

Если виртуальное окружение не активировано:
```bash
# Из корня проекта
source venv/bin/activate
cd bot
```

Установить зависимости (если ещё не установлены):
```bash
pip install -r requirements.txt
```

### 5. Запустить бота

```bash
python main.py
```

Должно появиться:
```
🚀 Запуск бота Seido...
[OK] Подключение к базе данных установлено
🔄 Запуск первичного парсинга забегов...
✅ Парсинг завершён. Добавлено забегов: X
✅ Бот Seido запущен!
```

### 6. Протестировать бота

1. Открой Telegram
2. Найди своего бота по юзернейму (например, `@seido_results_bot`)
3. Нажми "Запустить" или отправь `/start`
4. Пройди регистрацию (ФИО, дата рождения, город)
5. Проверь команды:
   - `/myresults` — мои результаты
   - `/stats` — общая статистика
   - `/help` — справка

### 7. Запуск в фоне (для продакшена)

**Вариант A: systemd (Linux)**
```bash
# Создай файл /etc/systemd/system/seido-bot.service
sudo nano /etc/systemd/system/seido-bot.service
```

Содержимое:
```ini
[Unit]
Description=Seido Telegram Bot
After=network.target

[Service]
Type=simple
User=твой_пользователь
WorkingDirectory=/home/skazhi/Документы/seido-landing/bot
Environment="PATH=/home/skazhi/Документы/seido-landing/venv/bin"
ExecStart=/home/skazhi/Документы/seido-landing/venv/bin/python main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl enable seido-bot
sudo systemctl start seido-bot
sudo systemctl status seido-bot
```

**Вариант B: screen/tmux**
```bash
screen -S seido-bot
cd /home/skazhi/Документы/seido-landing/bot
source ../venv/bin/activate
python main.py
# Нажми Ctrl+A, затем D для отсоединения
```

**Вариант C: nohup**
```bash
cd /home/skazhi/Документы/seido-landing/bot
source ../venv/bin/activate
nohup python main.py > bot.log 2>&1 &
```

## Проверка работы

### Логи
Смотри логи в консоли или файле `bot.log` (если использовал nohup).

### Тест команд
- `/start` — должен приветствовать и начать регистрацию
- `/myresults` — должен показать результаты (если есть в БД)
- `/stats` — должен показать общую статистику

### Ошибки

**Бот не запускается:**
- Проверь токен в `.env`
- Проверь, что все зависимости установлены
- Смотри ошибки в консоли

**Бот не отвечает:**
- Проверь, что бот запущен (процесс работает)
- Проверь интернет-соединение
- Проверь логи на ошибки

**Ошибка подключения к БД:**
- Проверь, что файл `seido.db` существует
- Проверь права доступа к файлу
- Убедись, что SQLite установлен

## Чеклист
- [ ] Токен бота получен от @BotFather
- [ ] Telegram ID администратора известен
- [ ] Файл `.env` создан и заполнен
- [ ] Зависимости установлены
- [ ] Бот запущен и отвечает на команды
- [ ] Регистрация работает
- [ ] Команды `/myresults` и `/stats` работают
- [ ] Бот настроен на автозапуск (опционально)

## Готово!
После выполнения всех пунктов отметь в `seido_plan.md`:
```
- [x] Бот запущен
```
