# Согласие на обработку персональных данных (для Telegram-бота)

## Текст для бота (встраивается в регистрацию)

---

### Вариант 1: Развёрнутый (рекомендуется)

```
📋 Перед началом работы — важное соглашение

Нажимая «Принять», вы даёте согласие на обработку 
ваших персональных данных:

• Фамилия, имя, дата рождения
• Город проживания
• Спортивные результаты на забегах

Цель обработки:
✅ Сохранение ваших результатов в истории
✅ Поиск ваших результатов по фамилии
✅ Формирование статистики и рейтингов
✅ Сравнение с другими бегунами

Ваши права:
📌 Вы можете запросить удаление всех данных в любой момент
📌 Вы можете отозвать согласие через поддержку
📌 Данные не передаются третьим лицам

Полная версия: https://skazhi.github.io/seido-landing/docs/offer.md

─────────────────────────────
[✅ Принять]  [❌ Отмена]
```

---

### Вариант 2: Краткий

```
⚠️ Важно!

Для работы бота нужно ваше согласие на обработку 
персональных данных (ФИО, город, результаты).

Данные хранятся в защищённой базе и используются 
только для показа вашей статистики.

Вы можете удалить данные в любой момент командой /delete.

Подробнее: https://skazhi.github.io/seido-landing/docs/offer.md

─────────────────────────────
[✅ Я согласен]  [❌ Отмена]
```

---

## Реализация в коде бота (handlers.py)

Добавь клавиатуру согласия:

```python
def get_consent_keyboard() -> ReplyKeyboardMarkup:
    """Клавиатура согласия на обработку ПДн"""
    keyboard = [
        [KeyboardButton(text="✅ Принять")],
        [KeyboardButton(text="❌ Отмена")],
    ]
    return ReplyKeyboardMarkup(keyboard=keyboard, resize_keyboard=True, one_time_keyboard=True)
```

Измени команду `/start`:

```python
@router.message(CommandStart())
async def cmd_start(message: types.Message, state: FSMContext):
    """Приветствие и запрос согласия"""
    user = await db.get_runner_by_telegram_id(message.from_user.id)

    if user:
        # Пользователь уже зарегистрирован и дал согласие
        await message.answer(
            f"👋 С возвращением, {user['first_name']}!\n\n"
            f"Добро пожаловать в Seido — Засейди свои результаты.\n\n"
            f"Используй кнопки ниже для навигации.",
            reply_markup=get_main_keyboard()
        )
    else:
        # Запрос согласия на обработку ПДн
        await message.answer(
            "👋 Привет! Я бот Seido.\n\n"
            "Я помогу тебе:\n"
            "• Найти все твои результаты в одном месте\n"
            "• Смотреть личные рекорды на любых дистанциях\n"
            "• Сравнивать себя с другими бегунами\n"
            "• Следить за предстоящими забегами\n\n"
            "─────────────────────────────\n\n"
            "📋 **Перед регистраацией — важное соглашение**\n\n"
            "Нажимая «✅ Принять», вы даёте согласие на обработку "
            "ваших персональных данных:\n\n"
            "• Фамилия, имя, дата рождения\n"
            "• Город проживания\n"
            "• Спортивные результаты на забегах\n\n"
            "**Цель обработки**:\n"
            "✅ Сохранение ваших результатов в истории\n"
            "✅ Поиск ваших результатов по фамилии\n"
            "✅ Формирование статистики и рейтингов\n\n"
            "**Ваши права**:\n"
            "📌 Вы можете запросить удаление всех данных в любой момент\n"
            "📌 Вы можете отозвать согласие через поддержку\n"
            "📌 Данные не передаются третьим лицам\n\n"
            "Полная версия: https://skazhi.github.io/seido-landing/docs/offer.md",
            reply_markup=get_consent_keyboard()
        )
        await state.set_state('waiting_for_consent')


@router.message(F.text == "✅ Принять")
async def process_consent(message: types.Message, state: FSMContext):
    """Обработка согласия"""
    # Сохраняем факт получения согласия в БД (опционально)
    await state.update_data(consent_given=True)
    
    await message.answer(
        "✅ Спасибо! Теперь давайте зарегистрируемся.\n\n"
        "Как тебя зовут?\n"
        "Напиши фамилию и имя (например: Иванов Иван)"
    )
    await state.set_state(Registration.waiting_for_name)


@router.message(F.text == "❌ Отмена")
async def cancel_consent(message: types.Message, state: FSMContext):
    """Отказ от согласия"""
    await message.answer(
        "❌ Понимаем. Без согласия мы не можем обрабатывать ваши данные.\n\n"
        "Если передумаете — просто напишите /start"
    )
    await state.clear()
```

---

## Команда для удаления данных (/delete)

Добавь в handlers.py:

```python
@router.message(Command("delete"))
async def cmd_delete(message: types.Message, state: FSMContext):
    """Удаление всех данных пользователя"""
    user = await db.get_runner_by_telegram_id(message.from_user.id)

    if not user:
        await message.answer("⚠️ У нас нет ваших данных. Сначала зарегистрируйтесь: /start")
        return

    await message.answer(
        "⚠️ **Вы уверены?**\n\n"
        "Это действие удалит:\n"
        "• Ваш профиль\n"
        "• Все ваши результаты\n"
        "• Подписки на забеги\n\n"
        "Восстановить данные будет невозможно.\n\n"
        "Нажмите «✅ Удалить» для подтверждения или «❌ Отмена»",
        reply_markup=get_delete_confirmation_keyboard()
    )
    await state.set_state('waiting_for_delete_confirm')


def get_delete_confirmation_keyboard() -> ReplyKeyboardMarkup:
    keyboard = [
        [KeyboardButton(text="✅ Удалить")],
        [KeyboardButton(text="❌ Отмена")],
    ]
    return ReplyKeyboardMarkup(keyboard=keyboard, resize_keyboard=True, one_time_keyboard=True)


@router.message(F.text == "✅ Удалить")
async def confirm_delete(message: types.Message, state: FSMContext):
    """Подтверждение удаления"""
    user = await db.get_runner_by_telegram_id(message.from_user.id)
    
    if user:
        # Удаляем данные (нужно добавить метод в db.py)
        await db.delete_runner(user['id'])
    
    await state.clear()
    
    await message.answer(
        "✅ Ваши данные удалены из базы.\n\n"
        "Если захотите вернуться — напишите /start"
    )


@router.message(F.text == "❌ Отмена")
async def cancel_delete(message: types.Message, state: FSMContext):
    """Отмена удаления"""
    await state.clear()
    await message.answer("✅ Удаление отменено. Ваши данные сохранены.")
```

---

## Метод удаления в db.py

Добавь в конец класса Database:

```python
async def delete_runner(self, runner_id: int) -> bool:
    """Удалить бегуна и все его данные"""
    async with self.db.execute(
        "DELETE FROM results WHERE runner_id = ?",
        (runner_id,)
    ):
        pass
    
    async with self.db.execute(
        "DELETE FROM race_subscriptions WHERE runner_id = ?",
        (runner_id,)
    ):
        pass
    
    async with self.db.execute(
        "DELETE FROM runners WHERE id = ?",
        (runner_id,)
    ):
        pass
    
    await self.db.commit()
    return True
```

---

## Чек-лист для compliance с 152-ФЗ

- [x] Текст согласия на обработку ПДн
- [x] Механизм получения согласия (кнопка в боте)
- [x] Механизм отзыва согласия (команда /delete)
- [x] Политика конфиденциальности (публичный документ)
- [ ] Уведомление Роскомнадзора (подать при масштабировании)
- [ ] Хранение доказательств согласия (опционально, рекомендуется)

---

*Документ составлен в соответствии с Федеральным законом № 152-ФЗ «О персональных данных».*
