# ✅ Проверка: База данных наполнила сайт

## Быстрая проверка

### 1. Проверьте API в браузере

Откройте в браузере следующие ссылки:

#### Проверка работы API:
```
http://seidorun.ru/api/api.php
```
**Ожидаемый результат:** JSON с информацией об API (не ошибка!)

#### Проверка забегов (предстоящие):
```
http://seidorun.ru/api/api.php?action=races_upcoming
```
**Ожидаемый результат:** JSON с массивом забегов:
```json
{
  "status": "ok",
  "data": [
    {
      "id": 1,
      "name": "Название забега",
      "date": "2026-03-15",
      ...
    }
  ]
}
```

#### Проверка забегов (прошедшие):
```
http://seidorun.ru/api/api.php?action=races_past
```
**Ожидаемый результат:** JSON с массивом прошедших забегов

#### Проверка рейтинга:
```
http://seidorun.ru/api/api.php?action=rating&distance=5 км
```
**Ожидаемый результат:** JSON с рейтингом бегунов на дистанции 5 км

---

### 2. Проверьте главную страницу сайта

Откройте в браузере:
```
http://seidorun.ru
```

**Что проверить:**
- ✅ Страница открывается (не 404)
- ✅ В разделе "Предстоящие забеги" есть забеги (или сообщение "Забегов пока нет")
- ✅ В разделе "Прошедшие забеги" есть забеги
- ✅ В разделе "Рейтинг бегунов" можно выбрать дистанцию и увидеть рейтинг

---

### 3. Проверьте данные в phpMyAdmin

#### Шаг 1: Откройте phpMyAdmin

1. Войдите в панель reg.ru
2. Найдите **"phpMyAdmin"** и нажмите **"Войти"**

#### Шаг 2: Проверьте таблицы

1. Выберите базу `u3426357_seido` (слева)
2. Проверьте таблицы:

**Таблица `runners` (бегуны):**
- Нажмите на таблицу `runners`
- Вкладка **"Просмотр"**
- Должны быть записи с бегунами
- Если пусто - данные не синхронизированы

**Таблица `races` (забеги):**
- Нажмите на таблицу `races`
- Вкладка **"Просмотр"**
- Должны быть записи с забегами
- Проверьте, что есть и предстоящие (дата >= сегодня), и прошедшие (дата < сегодня)

**Таблица `results` (результаты):**
- Нажмите на таблицу `results`
- Вкладка **"Просмотр"**
- Должны быть записи с результатами бегунов

#### Шаг 3: Проверьте количество записей

В каждой таблице внизу должно быть указано количество записей:
- `runners`: должно быть > 0
- `races`: должно быть > 0
- `results`: должно быть > 0 (если есть результаты)

---

### 4. Проверьте через SQL запросы

В phpMyAdmin можно выполнить SQL запросы:

#### Количество бегунов:
```sql
SELECT COUNT(*) as total FROM runners;
```

#### Количество забегов:
```sql
SELECT COUNT(*) as total FROM races;
```

#### Количество результатов:
```sql
SELECT COUNT(*) as total FROM results;
```

#### Предстоящие забеги:
```sql
SELECT * FROM races 
WHERE date >= CURDATE() 
ORDER BY date ASC 
LIMIT 10;
```

#### Прошедшие забеги:
```sql
SELECT * FROM races 
WHERE date < CURDATE() 
ORDER BY date DESC 
LIMIT 10;
```

---

## ❌ Если данные не отображаются

### Проблема 1: API возвращает ошибку

**Проверьте:**
1. Откройте `http://seidorun.ru/api/api.php` - должна быть информация об API
2. Если ошибка - проверьте настройки БД в `api/api.php`:
   ```php
   define('DB_HOST', 'localhost');
   define('DB_NAME', 'u3426357_seido');
   define('DB_USER', 'u3426357_Skazhi');
   define('DB_PASS', 'ваш_пароль');
   ```

### Проблема 2: API работает, но данных нет

**Проверьте:**
1. Есть ли данные в MySQL (через phpMyAdmin)
2. Если данных нет - нужно синхронизировать из SQLite

### Проблема 3: Данные в MySQL есть, но API не возвращает

**Проверьте:**
1. Правильность названий таблиц в `api/api.php`
2. Правильность названий полей в запросах
3. Логи ошибок в панели reg.ru

---

## 🔄 Как синхронизировать данные, если их нет

### Вариант 1: Экспорт/импорт через phpMyAdmin

1. **Экспортируйте данные из SQLite:**
   ```cmd
   cd "C:\Users\PC User\Documents\Seido\bot\scripts"
   python export_to_sql.py
   ```

2. **Импортируйте в phpMyAdmin:**
   - Откройте phpMyAdmin
   - Выберите базу `u3426357_seido`
   - Вкладка **"Импорт"**
   - Выберите файл: `C:\Users\PC User\Documents\Seido\bot\scripts\seido_export.sql`
   - Нажмите **"Вперёд"**

### Вариант 2: Прямая синхронизация (если настроен доступ)

1. **Настройте `.env`:**
   ```env
   MYSQL_HOST=31.31.196.247
   MYSQL_PORT=3306
   MYSQL_USER=u3426357_Skazhi
   MYSQL_PASSWORD=ваш_пароль
   MYSQL_DATABASE=u3426357_seido
   ```

2. **Запустите синхронизацию:**
   ```cmd
   cd "C:\Users\PC User\Documents\Seido\bot\scripts"
   python sync_to_mysql.py
   ```

---

## ✅ Чек-лист проверки

Пройдитесь по списку:

- [ ] API работает: `http://seidorun.ru/api/api.php` возвращает JSON
- [ ] Предстоящие забеги: `?action=races_upcoming` возвращает массив
- [ ] Прошедшие забеги: `?action=races_past` возвращает массив
- [ ] Рейтинг работает: `?action=rating&distance=5 км` возвращает данные
- [ ] Главная страница открывается: `http://seidorun.ru`
- [ ] На сайте отображаются забеги (если они есть в БД)
- [ ] В phpMyAdmin есть данные в таблице `runners`
- [ ] В phpMyAdmin есть данные в таблице `races`
- [ ] В phpMyAdmin есть данные в таблице `results`

---

## 🎯 Итоговая проверка

**Если всё работает:**
- ✅ Откройте `http://seidorun.ru`
- ✅ Прокрутите до раздела "Предстоящие забеги"
- ✅ Должны отображаться забеги из базы данных
- ✅ Прокрутите до раздела "Рейтинг бегунов"
- ✅ Выберите дистанцию и нажмите "Показать рейтинг"
- ✅ Должен отобразиться рейтинг из базы данных

**Поздравляю! Сайт работает с базой данных! 🎉**
