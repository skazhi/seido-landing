# 📤 Загрузка файлов на хостинг seidorun.ru

## Быстрый старт

### 1. Установите зависимости

```bash
cd bot
pip install python-dotenv
```

### 2. Настройте FTP доступ

Добавьте в файл `bot/.env` (или создайте его):

```env
# FTP настройки для загрузки на хостинг
FTP_HOST=seidorun.ru
FTP_USER=u3426357
FTP_PASSWORD=ваш_пароль_от_хостинга
FTP_PORT=21
FTP_REMOTE_DIR=/public_html
```

**Где взять данные:**
1. Войдите в панель управления reg.ru
2. Перейдите в раздел **Хостинг** → ваш хостинг
3. Найдите **FTP-доступ** или **Файловый менеджер**
4. Скопируйте:
   - **Хост:** обычно `seidorun.ru` или IP адрес
   - **Пользователь:** обычно начинается с `u` (например, `u3426357`)
   - **Пароль:** пароль от FTP (может отличаться от пароля панели)
   - **Директория:** обычно `/public_html` или `/www`

### 3. Запустите загрузку

**Windows:**
```cmd
cd scripts
upload_to_hosting.bat
```

**Или вручную:**
```bash
cd scripts
python upload_to_hosting.py
```

## Что будет загружено

Скрипт автоматически загрузит:

1. **index.html** → `public_html/index.html`
   - Главная страница сайта

2. **api/api.php** → `public_html/api/api.php`
   - API для работы с базой данных

3. **seido_logo.png** → `public_html/seido_logo.png`
   - Логотип Seido

## Проверка после загрузки

### 1. Проверьте главную страницу

Откройте в браузере:
- `http://seidorun.ru` или `https://seidorun.ru`

Должен открыться лендинг Seido.

### 2. Проверьте API

Откройте в браузере:
- `http://seidorun.ru/api/api.php`

Должно показать JSON с информацией об API.

### 3. Проверьте структуру файлов

Через FTP или файловый менеджер проверьте:
```
public_html/
├── index.html
├── seido_logo.png
└── api/
    └── api.php
```

## Устранение проблем

### Ошибка подключения к FTP

**Проблема:** `Connection refused` или `Timeout`

**Решение:**
- Проверьте правильность хоста (может быть IP вместо домена)
- Проверьте порт (обычно 21, но может быть другой)
- Проверьте, не блокирует ли файрвол соединение
- Попробуйте подключиться через FileZilla для проверки

### Ошибка авторизации

**Проблема:** `530 Login incorrect`

**Решение:**
- Проверьте правильность логина и пароля
- Убедитесь, что используете пароль от FTP, а не от панели управления
- Попробуйте сбросить пароль FTP в панели reg.ru

### Файлы не загружаются

**Проблема:** Файлы не появляются на сервере

**Решение:**
- Проверьте права доступа на сервере
- Убедитесь, что директория `/public_html` существует
- Проверьте, что используете правильный путь к корню сайта

### 404 ошибка после загрузки

**Проблема:** Сайт показывает 404

**Решение:**
1. Проверьте, что `index.html` находится в корне сайта (`public_html/`)
2. Проверьте настройки веб-сервера (должен искать `index.html`)
3. Убедитесь, что домен правильно привязан к хостингу

## Альтернативный способ: Ручная загрузка

Если автоматическая загрузка не работает, загрузите файлы вручную:

### Через файловый менеджер reg.ru

1. Войдите в панель управления reg.ru
2. Перейдите в **Файловый менеджер**
3. Откройте папку сайта (обычно `public_html/` или `www/`)
4. Загрузите файлы:
   - `index.html` → в корень
   - `api/api.php` → в папку `api/` (создайте, если нет)
   - `seido_logo.png` → в корень

### Через FileZilla

1. Скачайте FileZilla: https://filezilla-project.org/
2. Подключитесь к FTP серверу:
   - Хост: `seidorun.ru` или IP
   - Пользователь: из панели reg.ru
   - Пароль: из панели reg.ru
   - Порт: 21
3. Перетащите файлы:
   - Локально: `C:\Users\PC User\Documents\Seido\index.html`
   - На сервер: `/public_html/index.html`

## Следующие шаги

После загрузки файлов:

1. ✅ **Настройте базу данных** (см. `DEPLOY_DATABASE.md`)
2. ✅ **Настройте API** (обновите данные БД в `api/api.php`)
3. ✅ **Проверьте работу сайта**
4. ✅ **Настройте SSL** (HTTPS) в панели reg.ru

## Безопасность

⚠️ **Важно:**
- Не коммитьте файл `.env` с паролями в Git
- Используйте сильные пароли для FTP
- Регулярно обновляйте пароли
- Используйте SFTP вместо FTP, если возможно

## Дополнительная информация

- Инструкция по развёртыванию БД: `DEPLOY_DATABASE.md`
- Инструкция по настройке API: см. `api/api.php`
- Структура проекта: `PROJECT_STRUCTURE.md`
