# 🌐 Развёртывание сайта seidorun.ru на reg.ru

## Проблема: 404 Page not found

Если вы видите ошибку 404, это означает, что файлы сайта не загружены на хостинг или находятся не в той папке.

## Быстрое решение

### Шаг 1: Войдите в панель управления reg.ru

1. Откройте https://www.reg.ru/
2. Войдите в личный кабинет
3. Перейдите в раздел "Хостинг" → "Управление"
4. Выберите ваш хостинг

### Шаг 2: Откройте файловый менеджер

В панели управления найдите:
- **"Файловый менеджер"** или
- **"Файлы"** или
- **"File Manager"**

### Шаг 3: Определите корневую папку сайта

Обычно это одна из папок:
- `public_html/` - для основного домена
- `www/` - альтернативная папка
- `seidorun.ru/` - папка с именем домена
- `domains/seidorun.ru/public_html/` - стандартная структура

**Как проверить:**
- Если в папке есть файл `index.html` или `index.php` - это корневая папка
- Если папка пустая - это она и есть

### Шаг 4: Загрузите файлы сайта

**Файлы для загрузки:**
1. `index.html` - главная страница
2. `seido_logo.png` - логотип
3. `api/` - папка с API (если нужна)

**Способ 1: Через файловый менеджер (проще всего)**

1. В файловом менеджере перейдите в корневую папку сайта
2. Нажмите "Загрузить" или "Upload"
3. Выберите файлы:
   - `index.html`
   - `seido_logo.png`
4. Дождитесь загрузки

**Способ 2: Через FTP**

1. Узнайте данные FTP в панели управления:
   - Хост: обычно `ftp.seidorun.ru` или IP адрес
   - Пользователь: ваш логин
   - Пароль: ваш пароль
   - Порт: 21

2. Используйте FTP клиент (FileZilla, WinSCP):
   ```
   Хост: ftp.seidorun.ru
   Пользователь: ваш_логин
   Пароль: ваш_пароль
   Порт: 21
   ```

3. Подключитесь и загрузите файлы в корневую папку

**Способ 3: Через SSH (если доступен)**

```bash
# Подключитесь к серверу
ssh username@server186.hosting.reg.ru

# Перейдите в корневую папку
cd public_html  # или www, или domains/seidorun.ru/public_html

# Загрузите файлы через scp с локального компьютера
scp index.html username@server186.hosting.reg.ru:~/public_html/
scp seido_logo.png username@server186.hosting.reg.ru:~/public_html/
```

### Шаг 5: Создайте папку для API (если нужно)

1. В корневой папке создайте папку `api/`
2. Загрузите файл `api/api.php` в эту папку

### Шаг 6: Проверьте права доступа

Убедитесь, что файлы имеют правильные права:
- `index.html` - 644 (rw-r--r--)
- `api/api.php` - 644 (rw-r--r--)
- Папки - 755 (rwxr-xr-x)

В файловом менеджере обычно это делается через "Права доступа" или "Chmod"

### Шаг 7: Проверьте сайт

Откройте в браузере:
- `http://seidorun.ru/` - должен открыться сайт
- `http://seidorun.ru/api/api.php` - должен открыться API

## Структура файлов на сервере

После загрузки структура должна быть такой:

```
public_html/  (или www/)
├── index.html          ← Главная страница
├── seido_logo.png      ← Логотип
└── api/
    └── api.php         ← API (опционально)
```

## Настройка домена

### Если домен не привязан к хостингу:

1. В панели управления reg.ru:
   - Перейдите в "Домены"
   - Найдите `seidorun.ru`
   - Нажмите "Настроить" или "Управление DNS"

2. Убедитесь, что DNS записи указывают на хостинг:
   - A-запись: `@` → IP адрес сервера (узнайте в панели хостинга)
   - Или используйте NS-записи от reg.ru

3. Подождите 5-60 минут для обновления DNS

## Проверка работы

### 1. Проверьте главную страницу

Откройте: `http://seidorun.ru/`

Должна открыться главная страница Seido.

### 2. Проверьте API (если загружен)

Откройте: `http://seidorun.ru/api/api.php`

Должен вернуться JSON с информацией об API.

### 3. Проверьте логотип

Откройте: `http://seidorun.ru/seido_logo.png`

Должен отобразиться логотип.

## Устранение проблем

### Ошибка 404 после загрузки

**Причины:**
1. Файлы загружены не в ту папку
2. Файл называется не `index.html`
3. Неправильные права доступа

**Решение:**
- Проверьте, что `index.html` находится в корневой папке
- Проверьте права доступа (должны быть 644)

### Сайт открывается, но без стилей/изображений

**Причина:** Неправильные пути к файлам

**Решение:**
- Убедитесь, что `seido_logo.png` в той же папке, что и `index.html`
- Проверьте пути в `index.html` (должны быть относительные: `seido_logo.png`, а не `/path/to/seido_logo.png`)

### API не работает

**Причины:**
1. PHP не включен на хостинге
2. Неправильные настройки подключения к БД в `api.php`
3. База данных не создана

**Решение:**
1. Проверьте, что PHP включен (обычно включен по умолчанию)
2. Проверьте настройки в `api/api.php`:
   ```php
   define('DB_HOST', 'localhost');
   define('DB_NAME', 'u3426357_seido');
   define('DB_USER', 'u3426357_Skazhi');
   define('DB_PASS', 'your_password');
   ```
3. Создайте базу данных (см. `DEPLOY_DATABASE.md`)

## Чеклист развёртывания

- [ ] Вход в панель управления reg.ru выполнен
- [ ] Корневая папка сайта определена
- [ ] Файл `index.html` загружен в корневую папку
- [ ] Файл `seido_logo.png` загружен в корневую папку
- [ ] Папка `api/` создана (если нужна)
- [ ] Файл `api/api.php` загружен (если нужен)
- [ ] Права доступа установлены (644 для файлов, 755 для папок)
- [ ] Сайт открывается по адресу `http://seidorun.ru/`
- [ ] Логотип отображается
- [ ] API работает (если загружен)

## Дополнительные файлы

Если нужно загрузить дополнительные файлы:

- `docs/` - документация (опционально)
- `database/seido_mysql.sql` - схема БД (для импорта через phpMyAdmin)

## Готово! 🎉

После выполнения всех шагов сайт должен работать по адресу `http://seidorun.ru/`

Если что-то не работает, проверьте логи ошибок в панели управления хостингом.
