# 📤 Развёртывание базы данных на seidorun.ru

## Быстрый старт

### 1. Создайте MySQL базу данных на хостинге

1. Войдите в панель управления хостингом (reg.ru или другой)
2. Создайте базу данных MySQL
3. Запомните:
   - Имя базы данных
   - Имя пользователя
   - Пароль
   - Хост (обычно `localhost`)

### 2. Импортируйте схему базы данных

**Вариант A: Через phpMyAdmin**
1. Откройте phpMyAdmin
2. Выберите вашу базу данных
3. Перейдите на вкладку "Импорт"
4. Выберите файл `database/seido_mysql.sql`
5. Нажмите "Вперёд"

**Вариант B: Через командную строку (SSH)**
```bash
mysql -u username -p database_name < database/seido_mysql.sql
```

### 3. Настройте скрипт синхронизации

Создайте файл `.env` в папке `bot/` (если его нет):

```env
# MySQL настройки для синхронизации
MYSQL_HOST=localhost
MYSQL_USER=u3426357_Skazhi
MYSQL_PASSWORD=your_password_here
MYSQL_DATABASE=u3426357_seido
```

⚠️ **Важно:** Не коммитьте `.env` в Git! Он уже в `.gitignore`.

### 4. Установите зависимости

```bash
cd bot
pip install pymysql
```

### 5. Запустите синхронизацию

**Windows:**
```cmd
cd bot\scripts
sync_to_website.bat
```

**Или вручную:**
```bash
cd bot/scripts
python sync_to_mysql.py
```

## Автоматическая синхронизация

### Настройка автоматической синхронизации

Рекомендуется настроить автоматическую синхронизацию, чтобы данные на сайте всегда были актуальными.

**Windows - Планировщик задач:**
1. Откройте "Планировщик задач"
2. Создайте новую задачу
3. Триггер: каждые 2 часа
4. Действие: запуск `bot\scripts\sync_to_website.bat`

**Linux/Mac - Cron:**
```bash
# Синхронизация каждые 2 часа
0 */2 * * * cd /path/to/Seido/bot/scripts && python sync_to_mysql.py
```

## Проверка работы

### 1. Проверьте API

Откройте в браузере:
- `http://seidorun.ru/api/api.php` - информация об API
- `http://seidorun.ru/api/api.php?action=races_upcoming` - предстоящие забеги

### 2. Проверьте данные в MySQL

Войдите в phpMyAdmin и проверьте таблицы:
- `runners` - должны быть бегуны
- `races` - должны быть забеги
- `results` - должны быть результаты

## Структура данных

### Что синхронизируется:

1. **Бегуны (runners)**
   - Все пользователи бота
   - Профили с ФИО, датой рождения, городом

2. **Забеги (races)**
   - Все забеги из календаря
   - Название, дата, место, организатор

3. **Результаты (results)**
   - Все результаты бегунов
   - Время, место, дистанция

### Что НЕ синхронизируется:

- Временные данные (кэш)
- Логи
- Заявки на модерацию (можно добавить позже)

## Устранение проблем

### Ошибка подключения

```
❌ Ошибка подключения: Access denied
```

**Решение:**
- Проверьте правильность логина и пароля в `.env`
- Убедитесь, что пользователь MySQL имеет права на базу данных

### Данные не синхронизируются

**Проверьте:**
1. Существует ли файл `bot/seido.db`
2. Есть ли данные в SQLite (можно проверить через SQLite Browser)
3. Правильно ли настроены параметры подключения

### Ошибки при синхронизации результатов

Если результаты не синхронизируются:
- Убедитесь, что сначала синхронизированы бегуны и забеги
- Проверьте, что в SQLite есть связи между таблицами

## Безопасность

1. **Не храните пароли в коде** - используйте `.env`
2. **Ограничьте доступ к API** - настройте CORS в `api/api.php`
3. **Регулярно делайте бэкапы** MySQL базы данных
4. **Используйте HTTPS** для сайта

## Дополнительная информация

- Подробная инструкция: `bot/scripts/SYNC_TO_WEBSITE.md`
- Схема базы данных: `database/seido_mysql.sql`
- API документация: см. `api/api.php`
