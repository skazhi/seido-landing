# 🔄 Альтернативные способы синхронизации БД

## Проблема с SSH

Если SSH подключение не работает (Permission denied), используйте альтернативные методы.

---

## ✅ Вариант 1: Прямое подключение к MySQL (если разрешено)

### Шаг 1: Проверьте удалённый доступ в панели reg.ru

1. Зайдите в панель reg.ru
2. Перейдите в **"Базы данных MySQL"**
3. Найдите вашу базу `u3426357_seido`
4. Проверьте, есть ли опция **"Удалённый доступ"** или **"Remote MySQL"**
5. Если есть - включите её

### Шаг 2: Узнайте хост MySQL

В панели reg.ru в разделе "Базы данных MySQL" найдите:
- **Хост для удалённого подключения** (может быть `server186.hosting.reg.ru` или IP адрес)
- Или используйте IP адрес сервера: `31.31.196.247`

### Шаг 3: Обновите `.env`

Создайте файл `C:\Users\PC User\Documents\Seido\bot\.env`:

```env
MYSQL_HOST=31.31.196.247
MYSQL_PORT=3306
MYSQL_USER=u3426357_Skazhi
MYSQL_PASSWORD=ваш_пароль_MySQL
MYSQL_DATABASE=u3426357_seido
```

**Или попробуйте:**
```env
MYSQL_HOST=server186.hosting.reg.ru
MYSQL_PORT=3306
MYSQL_USER=u3426357_Skazhi
MYSQL_PASSWORD=ваш_пароль_MySQL
MYSQL_DATABASE=u3426357_seido
```

### Шаг 4: Запустите синхронизацию

```cmd
cd "C:\Users\PC User\Documents\Seido\bot\scripts"
python sync_to_mysql.py
```

---

## ✅ Вариант 2: Синхронизация через SQL-файл (экспорт/импорт)

Если прямое подключение не работает, можно экспортировать данные из SQLite и импортировать в MySQL.

### Шаг 1: Экспортируйте данные из SQLite

Создайте скрипт `export_to_sql.py` в папке `bot/scripts/`:

```python
import sqlite3
import json
from datetime import datetime

DB_PATH = "../seido.db"
OUTPUT_FILE = "seido_export.sql"

conn = sqlite3.connect(DB_PATH)
conn.row_factory = sqlite3.Row
cursor = conn.cursor()

with open(OUTPUT_FILE, 'w', encoding='utf-8') as f:
    f.write("-- Экспорт данных Seido из SQLite\n")
    f.write(f"-- Дата: {datetime.now()}\n\n")
    
    # Экспорт бегунов
    f.write("-- Бегуны\n")
    cursor.execute("SELECT * FROM runners")
    for row in cursor.fetchall():
        values = []
        for key in row.keys():
            val = row[key]
            if val is None:
                values.append("NULL")
            elif isinstance(val, str):
                values.append(f"'{val.replace("'", "''")}'")
            else:
                values.append(str(val))
        
        f.write(f"INSERT INTO runners ({', '.join(row.keys())}) VALUES ({', '.join(values)});\n")
    
    # Экспорт забегов
    f.write("\n-- Забеги\n")
    cursor.execute("SELECT * FROM races")
    for row in cursor.fetchall():
        # Аналогично для забегов
        pass
    
    # И так далее для всех таблиц

conn.close()
print(f"✅ Экспорт завершён: {OUTPUT_FILE}")
```

### Шаг 2: Импортируйте в MySQL через phpMyAdmin

1. Запустите скрипт экспорта
2. Откройте phpMyAdmin
3. Выберите базу `u3426357_seido`
4. Вкладка "Импорт" → выберите экспортированный файл → "Вперёд"

---

## ✅ Вариант 3: Включите SSH доступ в панели reg.ru

### Шаг 1: Включите SSH в панели

1. Зайдите в панель reg.ru
2. Найдите раздел **"SSH доступ"** или **"FTP/SSH"**
3. Включите SSH доступ (если выключен)
4. Установите пароль для SSH (может отличаться от пароля MySQL)

### Шаг 2: Используйте правильный пароль

SSH пароль может отличаться от пароля MySQL. Проверьте в панели reg.ru.

### Шаг 3: Попробуйте подключиться снова

```cmd
ssh -L 3307:localhost:3306 u3426357@server186.hosting.reg.ru
```

---

## ✅ Вариант 4: Использование FTP + PHP скрипт на сервере

### Шаг 1: Создайте PHP скрипт для синхронизации

Создайте файл `sync_from_sqlite.php` на сервере:

```php
<?php
// Загружает данные из SQLite файла в MySQL
// Загрузите этот файл на сервер через FTP

$sqlite_path = '/path/to/seido.db';
$mysql_host = 'localhost';
$mysql_db = 'u3426357_seido';
$mysql_user = 'u3426357_Skazhi';
$mysql_pass = 'your_password';

// Подключение к SQLite
$sqlite = new PDO("sqlite:$sqlite_path");

// Подключение к MySQL
$mysql = new PDO(
    "mysql:host=$mysql_host;dbname=$mysql_db;charset=utf8mb4",
    $mysql_user,
    $mysql_pass
);

// Синхронизация бегунов
$stmt = $sqlite->query("SELECT * FROM runners");
while ($row = $stmt->fetch(PDO::FETCH_ASSOC)) {
    // Вставка/обновление в MySQL
    // ...
}

echo "✅ Синхронизация завершена";
?>
```

### Шаг 2: Загрузите файлы на сервер

Через FTP загрузите:
- `seido.db` (локальная база данных)
- `sync_from_sqlite.php` (скрипт синхронизации)

### Шаг 3: Запустите скрипт

Откройте в браузере: `http://seidorun.ru/sync_from_sqlite.php`

---

## 🎯 Рекомендация

**Самый простой способ:** Попробуйте **Вариант 1** (прямое подключение к MySQL).

1. В панели reg.ru включите удалённый доступ к MySQL
2. Используйте IP адрес `31.31.196.247` как хост
3. Обновите `.env` файл
4. Запустите синхронизацию

Если это не сработает, используйте **Вариант 2** (экспорт/импорт через phpMyAdmin).

---

## 📞 Если ничего не помогает

1. Свяжитесь с поддержкой reg.ru
2. Уточните:
   - Как включить SSH доступ
   - Как настроить удалённый доступ к MySQL
   - Какой хост использовать для удалённого подключения к MySQL
