# 🚀 Развёртывание Seido на хостинге reg.ru

Полная инструкция по запуску сайта с базой данных.

---

## 📁 Файлы для загрузки

| Файл | Куда загружать |
|------|----------------|
| `index.html` | Корень сайта (`public_html/` или `www/`) |
| `api/api.php` | `public_html/api/` |
| `database/seido_mysql.sql` | (для импорта в phpMyAdmin) |

---

## Шаг 1: Создание базы данных

### 1.1 В панели reg.ru

1. Зайди в личный кабинет reg.ru
2. Перейди в **Хостинг** → твой хостинг
3. Найди раздел **Базы данных MySQL**
4. Нажми **Создать базу данных**

### 1.2 Заполни данные

| Поле | Значение |
|------|----------|
| Имя БД | `seido` (или `u123456_seido`) |
| Пользователь | `seido_user` |
| Пароль | придумай сложный пароль |

### 1.3 Запиши данные

Сохраните в надёжном месте:

```
Хост: localhost (или IP из панели)
Имя БД: u123456_seido
Пользователь: u123456_seido_user
Пароль: **************
```

---

## Шаг 2: Импорт схемы БД

### 2.1 Открой phpMyAdmin

1. В панели хостинга найди **phpMyAdmin**
2. Нажми **Войти**
3. Выбери свою базу данных (слева)

### 2.2 Импорт SQL

1. Перейди на вкладку **Импорт**
2. Нажми **Выберите файл**
3. Выбери `database/seido_mysql.sql`
4. Нажми **Вперёд**

### 2.3 Проверка

После импорта проверь, что появились таблицы:
- `runners`
- `races`
- `results`
- `race_subscriptions`
- `clubs`
- `club_members`

В базе будут тестовые данные (3 бегуна, 3 забега, 5 результатов).

---

## Шаг 3: Настройка API

### 3.1 Открой `api/api.php`

Найди строки (в начале файла):

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'seido');        // Измени на своё имя БД
define('DB_USER', 'seido_user');   // Измени на своего пользователя
define('DB_PASS', 'your_password'); // Измени на свой пароль
```

### 3.2 Замени на свои данные

Пример:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'u123456_seido');
define('DB_USER', 'u123456_seido_user');
define('DB_PASS', 'MySecurePassword123!');
```

### 3.3 Сохрани файл

---

## Шаг 4: Загрузка файлов на хостинг

### Вариант A: Через файловый менеджер reg.ru

1. Зайди в панель хостинга
2. **Файловый менеджер**
3. Перейди в папку сайта:
   - `/var/www/u123456/www/seidorun.ru/` или
   - `/home/u123456/www/seidorun.ru/` или
   - `public_html/`
4. Загрузи `index.html` в корень
5. Создай папку `api/`
6. Загрузи `api/api.php` в папку `api/`

### Вариант B: Через FTP (FileZilla)

1. Скачай FileZilla: https://filezilla-project.org/
2. Подключись:
   - **Хост:** `seidorun.ru` или `31.31.196.247`
   - **Логин:** из панели reg.ru
   - **Пароль:** из панели reg.ru
   - **Порт:** 21
3. Перетащи файлы:
   - `index.html` → `/public_html/`
   - `api/api.php` → `/public_html/api/`

---

## Шаг 5: Проверка API

### 5.1 Открой в браузере

1. **Главная API:** `https://seidorun.ru/api/api.php`
   
   Должно показать:
   ```json
   {
       "service": "Seido API",
       "version": "1.0",
       "status": "running",
       "endpoints": [...]
   }
   ```

2. **Предстоящие забеги:** `https://seidorun.ru/api/api.php?action=races_upcoming`
   
   Должно показать 3 тестовых забега

3. **Прошедшие забеги:** `https://seidorun.ru/api/api.php?action=races_past`
   
   Должно показать пустой список (забегов ещё не было)

4. **Рейтинг:** `https://seidorun.ru/api/api.php?action=rating&distance=5 км`
   
   Должно показать рейтинг на 5 км

---

## Шаг 6: Проверка сайта

### 6.1 Открой сайт

`https://seidorun.ru`

### 6.2 Что должно работать

- ✅ Лендинг отображается (неоновый дизайн, Tiffany цвета)
- ✅ Секция "Предстоящие забеги" — 3 карточки
- ✅ Секция "Прошедшие забеги" — сообщение "пока нет"
- ✅ Секция "Рейтинг" — выбери дистанцию, нажми "Показать рейтинг"
- ✅ Форма подписки работает (отправляет на Formspree)
- ✅ Кнопки соцсетей работают

---

## 🔧 Настройка DNS (если ещё не сделано)

### Для seidorun.ru

1. В панели reg.ru → домен `seidorun.ru`
2. DNS-серверы: `ns1.hosting.reg.ru`, `ns2.hosting.reg.ru`
3. A-запись: `@` → `31.31.196.247`
4. A-запись: `www` → `31.31.196.247`

### Для seidorun.online

1. В панели reg.ru → домен `seidorun.online`
2. DNS-серверы: `ns1.hosting.reg.ru`, `ns2.hosting.reg.ru`
3. A-запись: `@` → `31.31.196.247`
4. A-запись: `www` → `31.31.196.247`

**Жди 2-24 часа** для обновления DNS.

---

## 🔒 HTTPS (SSL-сертификат)

### В панели reg.ru

1. Хостинг → твой хостинг
2. Найди **SSL-сертификаты**
3. **Let's Encrypt** (бесплатно)
4. Выбери домен `seidorun.ru`
5. **Установить**

После установки сайт будет доступен по `https://`

---

## 📊 API Endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/api.php` | Информация об API |
| GET | `/api/api.php?action=races_upcoming` | Предстоящие забеги |
| GET | `/api/api.php?action=races_past` | Прошедшие забеги |
| GET | `/api/api.php?action=race&id=1` | Информация о забеге |
| GET | `/api/api.php?action=runner_results&telegram_id=123` | Результаты бегуна |
| GET | `/api/api.php?action=runner_bests&telegram_id=123` | Личные рекорды |
| GET | `/api/api.php?action=rating&distance=5 км` | Рейтинг на дистанции |
| GET | `/api/api.php?action=clubs` | Список клубов |
| GET | `/api/api.php?action=club&id=1` | Информация о клубе |

---

## 🐛 Решение проблем

### Ошибка "Database connection failed"

1. Проверь данные в `api/api.php` (хост, имя БД, пользователь, пароль)
2. Проверь, что БД существует в панели reg.ru
3. Проверь, что таблицы импортированы (phpMyAdmin)

### Ошибка 404 на `/api/api.php`

1. Проверь, что файл загружен в правильную папку
2. Путь должен быть: `public_html/api/api.php`

### Сайт не открывается

1. Проверь DNS (ping seidorun.ru)
2. Проверь, что файл `index.html` в корне сайта
3. Подожди 2-24 часа для обновления DNS

### API не загружает данные

1. Открой консоль браузера (F12)
2. Проверь ошибки JavaScript
3. Проверь, что API возвращает данные (открой URL API в браузере)

---

## 📝 Следующие шаги

1. ✅ Развернуть сайт на хостинге (эта инструкция)
2. ⬜ Настроить парсинг забегов (Python скрипты)
3. ⬜ Настроить Telegram-бота (на Raspberry Pi или VPS)
4. ⬜ Добавить реальные данные (вместо тестовых)
5. ⬜ Настроить Cloudflare Tunnel (для доступа к Raspberry Pi)

---

## 📞 Контакты

При возникновении проблем:
- Telegram: @Skazhi
- Email: seido.community@yandex.ru

---

**Удачи с запуском! 🏃‍♂️**
