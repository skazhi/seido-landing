# 🚀 Быстрая инструкция: Развёртывание БД на seidorun.ru

## Шаг 3: Создание базы данных на сервере

### Вариант A: Через phpMyAdmin (рекомендуется)

1. **Войдите в панель управления хостингом reg.ru**
   - Откройте https://www.reg.ru/user/
   - Войдите в свой аккаунт

2. **Откройте phpMyAdmin**
   - В панели управления найдите раздел "Базы данных" или "MySQL"
   - Нажмите на кнопку "phpMyAdmin" или "Управление БД"

3. **Выберите вашу базу данных**
   - В левом меню выберите базу `u3426357_seido`
   - Если базы нет, создайте её через панель управления хостингом

4. **Импортируйте SQL скрипт**
   - Нажмите на вкладку **"Импорт"** (вверху)
   - Нажмите кнопку **"Выберите файл"**
   - Найдите файл: `C:\Users\PC User\Documents\Seido\database\seido_mysql.sql`
   - Нажмите **"Вперёд"** или **"Выполнить"**
   - Дождитесь сообщения "Импорт успешно завершён"

5. **Проверьте создание таблиц**
   - В левом меню должны появиться таблицы:
     - `runners`
     - `races`
     - `results`
     - `race_subscriptions`
     - `clubs`
     - `club_members`

### Вариант B: Через SSH (если есть доступ)

```bash
# Подключитесь к серверу через SSH
ssh user@server186.hosting.reg.ru

# Импортируйте SQL скрипт
mysql -u u3426357_Skazhi -p u3426357_seido < /path/to/seido_mysql.sql
# Введите пароль, когда попросит
```

---

## Шаг 4: Настройка и запуск синхронизации

### 1. Создайте файл `.env` в папке `bot/`

**Важно:** Файл `.env` должен быть в папке `bot/`, а не в корне проекта!

1. Откройте папку: `C:\Users\PC User\Documents\Seido\bot\`
2. Создайте новый файл с именем `.env` (без расширения!)
3. Скопируйте туда следующие строки (замените `ваш_пароль` на реальный пароль):

```env
# MySQL настройки для синхронизации
MYSQL_HOST=localhost
MYSQL_USER=u3426357_Skazhi
MYSQL_PASSWORD=ваш_пароль
MYSQL_DATABASE=u3426357_seido
```

**Пример:**
```env
MYSQL_HOST=localhost
MYSQL_USER=u3426357_Skazhi
MYSQL_PASSWORD=fS9eO6gL2rbB5uM5
MYSQL_DATABASE=u3426357_seido
```

### 2. Установите зависимости (если ещё не установлены)

Откройте командную строку (PowerShell или CMD) и выполните:

```cmd
cd "C:\Users\PC User\Documents\Seido\bot"
pip install pymysql
```

### 3. Запустите синхронизацию

**Способ 1: Через batch-файл (проще всего)**

1. Откройте папку: `C:\Users\PC User\Documents\Seido\bot\scripts\`
2. Дважды кликните на файл `sync_to_website.bat`
3. Дождитесь завершения синхронизации

**Способ 2: Через командную строку**

```cmd
cd "C:\Users\PC User\Documents\Seido\bot\scripts"
python sync_to_mysql.py
```

### 4. Проверьте результат

После успешной синхронизации вы увидите:
```
✅ Синхронизация бегунов завершена: X записей
✅ Синхронизация забегов завершена: X записей
✅ Синхронизация результатов завершена: X записей
```

---

## Проверка работы

### 1. Проверьте API в браузере

Откройте:
- `http://seidorun.ru/api/api.php` - должна показать информацию об API
- `http://seidorun.ru/api/api.php?action=races_upcoming` - должны показаться забеги

### 2. Проверьте данные в phpMyAdmin

1. Откройте phpMyAdmin
2. Выберите базу `u3426357_seido`
3. Откройте таблицу `runners` - должны быть записи
4. Откройте таблицу `races` - должны быть забеги
5. Откройте таблицу `results` - должны быть результаты

---

## Возможные проблемы

### ❌ Ошибка: "Can't connect to MySQL server on 'localhost'"

**Проблема:** MySQL на хостинге reg.ru доступен только через `localhost` на самом сервере, а не с вашего компьютера.

**Решение 1: Использовать SSH туннель (рекомендуется)**

1. **Установите PuTTY или используйте встроенный SSH в Windows 10+**

2. **Создайте SSH туннель:**
   ```cmd
   ssh -L 3307:localhost:3306 u3426357@server186.hosting.reg.ru
   ```
   (Замените `u3426357` на ваш логин SSH, если отличается)

3. **Оставьте это окно открытым** и в другом окне обновите `.env`:
   ```env
   MYSQL_HOST=127.0.0.1
   MYSQL_PORT=3307
   MYSQL_USER=u3426357_Skazhi
   MYSQL_PASSWORD=ваш_пароль
   MYSQL_DATABASE=u3426357_seido
   ```

4. **Запустите синхронизацию** в новом окне терминала

**Решение 2: Использовать удалённый хост (если разрешено)**

В панели reg.ru проверьте, разрешены ли удалённые подключения к MySQL. Если да, используйте:
```env
MYSQL_HOST=server186.hosting.reg.ru
# или IP адрес сервера
```

**Решение 3: Запускать синхронизацию на сервере**

1. Загрузите файлы на сервер через FTP
2. Подключитесь по SSH
3. Запустите синхронизацию на сервере

### Ошибка: "Access denied for user"
**Решение:** Проверьте правильность пароля в файле `.env`

### Ошибка: "Table doesn't exist"
**Решение:** Убедитесь, что вы выполнили Шаг 3 (импорт SQL скрипта)

### Ошибка: "No module named 'pymysql'"
**Решение:** Выполните: `pip install pymysql`

### Ошибка: "No such file or directory: seido.db"
**Решение:** Убедитесь, что файл `bot/seido.db` существует (это локальная база данных бота)

---

## Готово! 🎉

После выполнения всех шагов:
- ✅ База данных создана на хостинге
- ✅ Данные синхронизированы
- ✅ API работает
- ✅ Сайт отображает данные

Теперь можно открыть `http://seidorun.ru` и увидеть забеги и результаты!
