# 🔧 Решение проблемы подключения к MySQL

## Проблема

Ошибка: `Can't connect to MySQL server on 'localhost'`

Это происходит потому, что MySQL на хостинге reg.ru доступен только через `localhost` **на самом сервере**, а не с вашего компьютера.

---

## ✅ Решение 1: SSH туннель (рекомендуется)

### Шаг 1: Узнайте данные для SSH подключения

В панели reg.ru найдите:
- **SSH логин** (обычно `u3426357` или похожий)
- **SSH пароль** (может отличаться от пароля MySQL)
- **SSH хост** (обычно `server186.hosting.reg.ru` или IP адрес)

### Шаг 2: Создайте SSH туннель

**Вариант A: Через встроенный SSH в Windows 10+**

Откройте PowerShell или CMD и выполните:

```cmd
ssh -L 3307:localhost:3306 u3426357@server186.hosting.reg.ru
```

**Важно:** 
- Замените `u3426357` на ваш SSH логин
- Замените `server186.hosting.reg.ru` на ваш SSH хост
- `3307` - это локальный порт (можете использовать другой, например 3308)
- `3306` - это порт MySQL на сервере

После выполнения команды:
1. Введите SSH пароль (если попросит)
2. **Оставьте это окно открытым!** (туннель работает, пока окно открыто)

### Шаг 3: Обновите файл `.env`

Создайте или обновите файл `C:\Users\PC User\Documents\Seido\bot\.env`:

```env
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3307
MYSQL_USER=u3426357_Skazhi
MYSQL_PASSWORD=ваш_пароль_MySQL
MYSQL_DATABASE=u3426357_seido
```

**Важно:** 
- `MYSQL_HOST=127.0.0.1` (не localhost!)
- `MYSQL_PORT=3307` (тот же порт, что в SSH команде)

### Шаг 4: Запустите синхронизацию

В **новом окне** терминала (старое оставьте открытым):

```cmd
cd "C:\Users\PC User\Documents\Seido\bot\scripts"
python sync_to_mysql.py
```

---

## ✅ Решение 2: Проверьте удалённый доступ к MySQL

### Шаг 1: В панели reg.ru

1. Зайдите в раздел **"Базы данных MySQL"**
2. Найдите вашу базу данных
3. Проверьте, есть ли опция **"Удалённый доступ"** или **"Remote access"**
4. Если есть - включите её и добавьте ваш IP адрес

### Шаг 2: Узнайте хост MySQL

В панели reg.ru найдите:
- **Хост MySQL** (может быть `server186.hosting.reg.ru` или IP адрес)
- Это **НЕ** `localhost`!

### Шаг 3: Обновите `.env`

```env
MYSQL_HOST=server186.hosting.reg.ru
MYSQL_PORT=3306
MYSQL_USER=u3426357_Skazhi
MYSQL_PASSWORD=ваш_пароль
MYSQL_DATABASE=u3426357_seido
```

---

## ✅ Решение 3: Запуск синхронизации на сервере

Если у вас есть SSH доступ, можно запустить синхронизацию прямо на сервере:

### Шаг 1: Загрузите файлы на сервер

Через FTP загрузите:
- `bot/scripts/sync_to_mysql.py`
- `bot/seido.db` (локальная база данных)
- `bot/.env` (с настройками)

### Шаг 2: Подключитесь по SSH

```cmd
ssh u3426357@server186.hosting.reg.ru
```

### Шаг 3: Установите зависимости

```bash
pip3 install pymysql aiosqlite python-dotenv
```

### Шаг 4: Запустите синхронизацию

```bash
cd /path/to/Seido/bot/scripts
python3 sync_to_mysql.py
```

---

## 🔍 Как узнать данные для подключения?

### В панели reg.ru:

1. Зайдите в **"Хостинг"** → ваш хостинг
2. Найдите раздел **"Базы данных MySQL"**
3. Там будут указаны:
   - Имя базы данных
   - Пользователь
   - Пароль
   - Хост (обычно `localhost` для подключения с сервера)

### Для SSH:

1. В панели reg.ru найдите раздел **"SSH доступ"** или **"FTP/SSH"**
2. Там будут указаны:
   - SSH логин
   - SSH пароль
   - SSH хост (обычно `server186.hosting.reg.ru`)

---

## ⚠️ Важные замечания

1. **SSH туннель должен быть открыт** во время синхронизации
2. Если туннель закрылся - переподключитесь и запустите синхронизацию снова
3. **Не закрывайте окно** с SSH туннелем, пока идёт синхронизация
4. Для автоматической синхронизации лучше использовать **Решение 3** (запуск на сервере)

---

## 🆘 Если ничего не помогает

1. Проверьте, что база данных создана и таблицы импортированы
2. Проверьте правильность пароля MySQL
3. Свяжитесь с поддержкой reg.ru и уточните:
   - Разрешён ли удалённый доступ к MySQL
   - Какой хост использовать для удалённого подключения
