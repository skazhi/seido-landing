# 🚀 Полная инструкция: Загрузка сайта и БД на seidorun.ru

## 📋 Что нужно сделать

1. ✅ Создать базу данных на хостинге
2. ✅ Импортировать схему БД
3. ✅ Загрузить файлы сайта (index.html, api/api.php)
4. ✅ Синхронизировать данные из SQLite в MySQL

---

## Шаг 1: Создание базы данных на хостинге

### 1.1 Войдите в панель reg.ru

1. Откройте https://www.reg.ru/user/
2. Войдите в свой аккаунт
3. Перейдите в **"Хостинг"** → ваш хостинг

### 1.2 Создайте базу данных MySQL

1. Найдите раздел **"Базы данных MySQL"**
2. Нажмите **"Создать базу данных"** или **"Добавить"**
3. Заполните данные:
   - **Имя БД:** `u3426357_seido` (или просто `seido`)
   - **Пользователь:** `u3426357_Skazhi` (или создайте нового)
   - **Пароль:** придумайте сложный пароль (запишите его!)
4. Нажмите **"Создать"**

### 1.3 Запишите данные подключения

Сохраните в надёжном месте:
```
Хост: localhost (для подключения с сервера)
Имя БД: u3426357_seido
Пользователь: u3426357_Skazhi
Пароль: [ваш пароль]
```

---

## Шаг 2: Импорт схемы базы данных

### 2.1 Откройте phpMyAdmin

1. В панели reg.ru найдите **"phpMyAdmin"**
2. Нажмите **"Войти"** или **"Открыть"**

### 2.2 Выберите базу данных

1. В левом меню выберите базу `u3426357_seido`
2. Если базы нет в списке - обновите страницу

### 2.3 Импортируйте SQL скрипт

1. Нажмите на вкладку **"Импорт"** (вверху)
2. Нажмите кнопку **"Выберите файл"**
3. Найдите файл: `C:\Users\PC User\Documents\Seido\database\seido_mysql.sql`
4. Нажмите **"Вперёд"** или **"Выполнить"**
5. Дождитесь сообщения **"Импорт успешно завершён"**

### 2.4 Проверьте создание таблиц

В левом меню должны появиться таблицы:
- ✅ `runners` (бегуны)
- ✅ `races` (забеги)
- ✅ `results` (результаты)
- ✅ `race_subscriptions` (подписки на забеги)
- ✅ `clubs` (клубы)
- ✅ `club_members` (члены клубов)

---

## Шаг 3: Загрузка файлов сайта

### 3.1 Подготовьте файлы

Убедитесь, что у вас есть:
- ✅ `index.html` (главная страница)
- ✅ `api/api.php` (API для сайта)
- ✅ `seido_logo.png` (логотип, если есть)

### 3.2 Загрузите через FTP

**Вариант A: Через FileZilla или другой FTP клиент**

1. Установите FileZilla (если нет): https://filezilla-project.org/
2. Подключитесь к FTP:
   - **Хост:** `server186.hosting.reg.ru` (или из панели reg.ru)
   - **Пользователь:** `u3426357` (ваш FTP логин)
   - **Пароль:** (ваш FTP пароль)
   - **Порт:** `21`
3. Перейдите в папку `public_html/` или `www/`
4. Загрузите файлы:
   - `index.html` → в корень `public_html/`
   - `api/api.php` → в папку `public_html/api/` (создайте папку, если нет)
   - `seido_logo.png` → в корень `public_html/` (если есть)

**Вариант B: Через панель reg.ru (Файловый менеджер)**

1. В панели reg.ru найдите **"Файловый менеджер"** или **"Файлы"**
2. Перейдите в папку `public_html/` или `www/`
3. Загрузите файлы через веб-интерфейс

**Вариант C: Через скрипт (если настроен)**

```cmd
cd "C:\Users\PC User\Documents\Seido\scripts"
python upload_to_hosting.py
```

### 3.3 Настройте API

Откройте файл `api/api.php` на сервере и проверьте настройки подключения к БД:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'u3426357_seido');
define('DB_USER', 'u3426357_Skazhi');
define('DB_PASS', 'ваш_пароль_MySQL');
```

**Важно:** Замените `ваш_пароль_MySQL` на реальный пароль из Шага 1.3!

---

## Шаг 4: Синхронизация данных из SQLite в MySQL

### 4.1 Создайте файл `.env`

Создайте файл `C:\Users\PC User\Documents\Seido\bot\.env`:

```env
MYSQL_HOST=31.31.196.247
MYSQL_PORT=3306
MYSQL_USER=u3426357_Skazhi
MYSQL_PASSWORD=ваш_пароль_MySQL
MYSQL_DATABASE=u3426357_seido
```

**Важно:** 
- Замените `ваш_пароль_MySQL` на реальный пароль
- Если удалённый доступ не работает, используйте экспорт/импорт (см. ниже)

### 4.2 Попробуйте прямую синхронизацию

```cmd
cd "C:\Users\PC User\Documents\Seido\bot\scripts"
python sync_to_mysql.py
```

### 4.3 Если не работает - используйте экспорт/импорт

**Шаг 1: Экспортируйте данные из SQLite**

```cmd
cd "C:\Users\PC User\Documents\Seido\bot\scripts"
python export_to_sql.py
```

Это создаст файл `seido_export.sql` с данными.

**Шаг 2: Импортируйте в phpMyAdmin**

1. Откройте phpMyAdmin
2. Выберите базу `u3426357_seido`
3. Вкладка **"Импорт"**
4. Выберите файл `C:\Users\PC User\Documents\Seido\bot\scripts\seido_export.sql`
5. Нажмите **"Вперёд"**

---

## Шаг 5: Проверка работы

### 5.1 Проверьте сайт

Откройте в браузере:
- `http://seidorun.ru` - должна открыться главная страница
- `http://seidorun.ru/api/api.php` - должна показать информацию об API

### 5.2 Проверьте API

Откройте:
- `http://seidorun.ru/api/api.php?action=races_upcoming` - должны показаться забеги
- `http://seidorun.ru/api/api.php?action=races_past` - прошедшие забеги

### 5.3 Проверьте данные в MySQL

1. Откройте phpMyAdmin
2. Выберите базу `u3426357_seido`
3. Откройте таблицу `runners` - должны быть бегуны
4. Откройте таблицу `races` - должны быть забеги
5. Откройте таблицу `results` - должны быть результаты

---

## ⚠️ Важные замечания

1. **Пароли:** Не забывайте записывать пароли в надёжном месте
2. **Безопасность:** Не публикуйте пароли в Git или публичных местах
3. **Бэкапы:** Регулярно делайте резервные копии базы данных через phpMyAdmin

---

## 🆘 Если что-то не работает

### Сайт показывает 404

- Проверьте, что файлы загружены в правильную папку (`public_html/` или `www/`)
- Проверьте права доступа к файлам (должны быть 644 для файлов, 755 для папок)

### API не работает

- Проверьте настройки подключения к БД в `api/api.php`
- Проверьте, что база данных создана и таблицы импортированы
- Проверьте логи ошибок в панели reg.ru

### Данные не синхронизируются

- Используйте экспорт/импорт через phpMyAdmin (Шаг 4.3)
- Проверьте, что файл `bot/seido.db` существует и содержит данные

---

## ✅ Готово!

После выполнения всех шагов:
- ✅ Сайт работает на http://seidorun.ru
- ✅ База данных создана и заполнена
- ✅ API работает и отдаёт данные
- ✅ Все данные синхронизированы

🎉 Поздравляю! Ваш сайт готов к работе!
