# 🏃‍♂️ Синхронизация забегов на сайт

**Цель:** Добавить все напарсенные забеги на сайт seidorun.ru

---

## 🚀 Быстрый способ

### Вариант 1: Через скрипт (рекомендуется)

1. **Открой терминал в папке `bot/scripts`**

2. **Запусти скрипт:**
   ```bash
   sync_races_only.bat
   ```
   
   Или через Python:
   ```bash
   python sync_races_only.py
   ```

3. **Проверь результат:**
   - Открой: https://seidorun.ru/api/api.php?action=races_upcoming
   - Должны вернуться все предстоящие забеги

---

## ⚙️ Настройка подключения

Если нужно изменить настройки MySQL, отредактируй `bot/.env`:

```env
MYSQL_HOST=31.31.196.247
MYSQL_PORT=3306
MYSQL_USER=u3426357_Skazhi
MYSQL_PASSWORD=твой_пароль
MYSQL_DATABASE=u3426357_seido
```

Или измени в `bot/scripts/sync_races_only.py`:

```python
MYSQL_CONFIG = {
    'host': '31.31.196.247',
    'port': 3306,
    'user': 'u3426357_Skazhi',
    'password': 'твой_пароль',
    'database': 'u3426357_seido',
    'charset': 'utf8mb4',
    'autocommit': False
}
```

---

## ✅ Что синхронизируется

- ✅ **Все забеги** из локальной SQLite базы (`bot/seido.db`)
- ✅ **Только забеги** (без бегунов и результатов)
- ✅ Автоматическое обновление существующих забегов
- ✅ Добавление новых забегов

---

## 🔍 Проверка результата

### 1. Проверь API

Открой в браузере:
```
https://seidorun.ru/api/api.php?action=races_upcoming
```

Должен вернуться JSON с забегами:
```json
{
  "status": "ok",
  "count": 50,
  "data": [
    {
      "id": 1,
      "name": "Название забега",
      "date": "2026-03-15",
      "location": "Москва",
      ...
    }
  ]
}
```

### 2. Проверь сайт

Открой: https://seidorun.ru

Прокрути до раздела **"Предстоящие забеги"** — должны отображаться карточки забегов.

---

## 🆘 Решение проблем

### Ошибка подключения к MySQL

**Проблема:** `Can't connect to MySQL server`

**Решение:**
1. Проверь настройки в `bot/.env`
2. Убедись, что MySQL доступен с твоего IP
3. Проверь пароль в панели reg.ru

### Забеги не отображаются на сайте

**Проблема:** API возвращает пустой массив

**Решение:**
1. Проверь, что забеги есть в SQLite:
   ```bash
   sqlite3 bot/seido.db "SELECT COUNT(*) FROM races WHERE date >= date('now');"
   ```
2. Проверь, что синхронизация прошла успешно
3. Проверь, что в MySQL забеги с датой >= сегодня

### Забеги не видны в разделе "Предстоящие"

**Проблема:** Забеги есть в базе, но не отображаются

**Решение:**
1. Открой консоль браузера (F12)
2. Проверь, есть ли ошибки при загрузке API
3. Проверь, что `is_active = 1` в базе данных

---

## 📊 Статистика

После синхронизации скрипт покажет:
- Количество добавленных забегов
- Количество обновленных забегов
- Количество ошибок (если есть)

---

## 🔄 Регулярная синхронизация

Чтобы забеги всегда были актуальными на сайте:

1. **После каждого парсинга** запускай `sync_races_only.bat`
2. Или настрой автоматическую синхронизацию (cron/task scheduler)

---

**Время синхронизации:** 1-2 минуты (зависит от количества забегов)

*Готово! Теперь все забеги на сайте 🎉*
