# 🚀 Быстрый старт Seido

## Этап 0: Подготовка (сегодня)

### 1. Telegram-канал
1. Создай канал в Telegram
2. Юзернейм: `@seido_run` (или похожий)
3. Опубликуй приветственный пост (см. `telegram_content.md`)

### 2. Лендинг
1. Открой `index.html` в браузере
2. Замени `YOUR_FORM_ID` в форме на свой Formspree ID
3. Загрузи на GitHub Pages или Vercel (бесплатно)

**GitHub Pages:**
```bash
# Создай репозиторий seido на GitHub
# Загрузи index.html
# Включи GitHub Pages в настройках
# Сайт будет доступен по: https://твой-ник.github.io/seido/
```

**Vercel:**
```bash
# Установи Vercel CLI
npm i -g vercel

# В папке проекта
vercel --prod
```

### 3. Домен (опционально)
Проверь доступность:
- seido.ru (~200 руб/год)
- seido.app (~$15/год)
- seido.xyz (~$5/год)

---

## Этап 1: Бот (1-2 дня)

### 1. Получить токен бота
1. Открой @BotFather в Telegram
2. `/newbot`
3. Назови бота: `Seido Bot`
4. Юзернейм: `@seido_results_bot` (или похожий)
5. Скопируй токен

### 2. Настроить базу данных

**Вариант A: Локально (для теста)**
```bash
# Установи PostgreSQL
# Создай базу данных
createdb seido

# Выполни схему
psql -d seido -f bot/schema.sql
```

**Вариант B: Облако (бесплатно)**
- [Render](https://render.com/) - бесплатный PostgreSQL
- [Railway](https://railway.app/) - $5 кредит бесплатно
- [Neon](https://neon.tech/) - бесплатный tier

### 3. Настроить окружение
```bash
cd bot
copy .env.example .env
```

Открой `.env` и заполни:
```
BOT_TOKEN=1234567890:AABBccDDeeFFggHHiiJJkkLLmmNNooP
DATABASE_URL=postgresql://user:password@host:5432/seido
ADMIN_ID=твой_telegram_id
```

**Как узнать свой Telegram ID:**
1. Напиши боту @userinfobot
2. Он пришлёт твой ID

### 4. Установить зависимости
```bash
cd bot
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Mac/Linux

pip install -r requirements.txt
```

### 5. Запустить бота
```bash
python main.py
```

### 6. Протестировать
1. Открой бота в Telegram
2. Нажми `/start`
3. Пройди регистрацию
4. Проверь команды

---

## Этап 2: Наполнение базы (2-3 дня)

### 1. Собрать протоколы
Найди протоколы забегов:
- 5verest.ru
- s95.ru
- rhr-marathon.ru
- и другие

### 2. Парсинг (опционально)
Если протоколы в PDF/Excel, можно написать скрипт для парсинга.

### 3. Ручное добавление
Добавь первые результаты вручную через SQL:
```sql
-- Пример добавления бегуна
INSERT INTO runners (first_name, last_name, birth_date, gender, city)
VALUES ('Иван', 'Иванов', '1990-05-15', 'M', 'Москва');

-- Пример добавления забега
INSERT INTO races (name, date, location, organizer, race_type)
VALUES ('Пятерка в Парке', '2026-03-01', 'Парк Горького, Москва', '5верст', 'шоссе');

-- Пример добавления результата
INSERT INTO results (runner_id, race_id, distance, finish_time_seconds, overall_place, total_runners)
VALUES (1, 1, '5 км', 1500, 42, 350);
```

---

## Этап 3: Запуск для комьюнити (неделя 2)

### 1. Пригласить тестеров
Отправь приглашение в свой беговой чат (см. `telegram_content.md`)

### 2. Собрать обратную связь
Создай чат тестеров, собирай баги и пожелания.

### 3. Итерации
Исправляй баги, добавляй фичи по запросу.

---

## 📊 Чеклист запуска

- [ ] Telegram-канал создан, первый пост опубликован
- [ ] Лендинг опубликован (GitHub Pages / Vercel)
- [ ] Бот получен от @BotFather
- [ ] База данных развёрнута
- [ ] Бот запущен и отвечает на команды
- [ ] В базе есть первые забеги и результаты
- [ ] 10+ тестеров попробовали бота
- [ ] Собрана обратная связь

---

## 🆘 Если что-то пошло не так

**Бот не запускается:**
- Проверь токен в `.env`
- Проверь, что PostgreSQL запущен
- Смотри логи в консоли

**Бот не отвечает на команды:**
- Проверь, что импортирован `handlers.py`
- Проверь, что `dp.include_router(router)` вызван

**Ошибка подключения к БД:**
- Проверь `DATABASE_URL`
- Убедись, что таблица создана (`schema.sql`)

---

## 📞 Связь

Если нужны помощь или совет — пиши в чат тестеров.

**Удачи! 🏃‍♂️**
