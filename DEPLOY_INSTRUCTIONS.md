# Инструкции по настройке и деплою Seido

## 📋 Что нужно сделать

### 1. Подготовка файлов

У вас уже есть:
- ✅ `index.html` — главная страница с полной информацией о Seido
- ✅ `api/api.php` — API для работы с базой данных

### 2. Загрузка на хостинг

#### Вариант A: Через файловый менеджер reg.ru

1. Войдите в панель управления reg.ru
2. Откройте **Файловый менеджер**
3. Перейдите в папку `www/seidorun.ru/` (или `public_html/`)
4. Загрузите файлы:
   - `index.html` → в корень `www/seidorun.ru/`
   - `api/api.php` → в папку `www/seidorun.ru/api/` (создайте папку `api`, если её нет)

#### Вариант B: Через FTP

1. Используйте FTP-клиент (FileZilla, WinSCP)
2. Подключитесь к серверу:
   - **Хост:** `seidorun.ru` или IP сервера
   - **Пользователь:** `u3426357` (или ваш FTP-логин)
   - **Пароль:** ваш FTP-пароль
   - **Порт:** `21`
3. Загрузите файлы:
   - `index.html` → `/public_html/` или `/www/seidorun.ru/`
   - `api/api.php` → `/public_html/api/` или `/www/seidorun.ru/api/`

### 3. Настройка API

Откройте файл `api/api.php` и проверьте настройки подключения к БД:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'u3426357_seido');        // Ваше имя БД
define('DB_USER', 'u3426357_Skazhi');       // Ваш пользователь БД
define('DB_PASS', 'EhmN083fA1108nv1!');     // Ваш пароль БД
define('DB_CHARSET', 'utf8mb4');
```

**Важно:** Замените значения на актуальные из панели reg.ru:
- Имя БД можно найти в разделе **Базы данных MySQL**
- Пользователь и пароль — там же

### 4. Проверка работы

1. Откройте сайт: `https://seidorun.ru`
2. Проверьте, что страница загружается корректно
3. Проверьте API: `https://seidorun.ru/api/api.php`
   - Должен вернуться JSON с информацией об API

### 5. Настройка базы данных (если нужно)

Если база данных ещё не создана:

1. Войдите в **phpMyAdmin** через панель reg.ru
2. Создайте новую базу данных (например, `u3426357_seido`)
3. Импортируйте структуру из `database/seido_mysql.sql`
4. Обновите настройки в `api/api.php`

## 🔧 Дополнительные настройки

### SSL сертификат

Если сайт открывается по `http://`, но не по `https://`:

1. В панели reg.ru найдите раздел **SSL-сертификаты**
2. Установите бесплатный Let's Encrypt сертификат
3. Настройте редирект с HTTP на HTTPS

### Кэширование

Для улучшения производительности можно настроить кэширование в `.htaccess`:

```apache
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/html "access plus 1 hour"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
</IfModule>
```

## ✅ Чеклист

- [ ] Файл `index.html` загружен на хостинг
- [ ] Файл `api/api.php` загружен в папку `api/`
- [ ] Настройки БД в `api.php` обновлены
- [ ] Сайт открывается по адресу `https://seidorun.ru`
- [ ] API работает: `https://seidorun.ru/api/api.php`
- [ ] SSL сертификат установлен (опционально)

## 🆘 Решение проблем

### Страница не открывается (404)

- Проверьте, что файл `index.html` находится в правильной папке
- Убедитесь, что файл называется именно `index.html` (не `Index.html`)

### API не работает

- Проверьте настройки подключения к БД в `api/api.php`
- Убедитесь, что база данных создана и доступна
- Проверьте логи ошибок в панели reg.ru

### Стили не применяются

- Очистите кэш браузера (Ctrl+F5)
- Проверьте, что CSS код находится внутри `<style>` тега в `index.html`

## 📞 Поддержка

Если возникли проблемы:
- Email: seido.community@yandex.ru
- Telegram: @Skazhi
